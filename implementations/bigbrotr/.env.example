# ============================================================================
# BigBrotr Environment Configuration Template
# ============================================================================
# Copy this file to .env and configure your environment variables
# Command: cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION (REQUIRED)
# ----------------------------------------------------------------------------

# PostgreSQL Database Password
# REQUIRED: Must be set for ConnectionPool to function
# Security: Minimum 12 characters recommended
# Used by: src/core/pool.py
DB_PASSWORD=your_secure_password_here

# ----------------------------------------------------------------------------
# DATABASE CONNECTION (Optional - defaults provided)
# ----------------------------------------------------------------------------

# PostgreSQL Host
# Default: localhost (configured in yaml/core/brotr.yaml)
# DB_HOST=localhost

# PostgreSQL Port
# Default: 5432 (configured in yaml/core/brotr.yaml)
# DB_PORT=5432

# PostgreSQL Database Name
# Default: brotr (configured in yaml/core/brotr.yaml)
# DB_NAME=brotr

# PostgreSQL User
# Default: admin (configured in yaml/core/brotr.yaml)
# DB_USER=admin

# ----------------------------------------------------------------------------
# NETWORK CONFIGURATION (Optional)
# ----------------------------------------------------------------------------

# SOCKS5 Proxy URL (for Tor support)
# Optional: Uncomment to route traffic through Tor
# Format: socks5://host:port
# Example: socks5://localhost:9050
# Used by: Synchronizer service for relay connections
# SOCKS5_PROXY_URL=socks5://localhost:9050

# ----------------------------------------------------------------------------
# APPLICATION CONFIGURATION (Optional - configured in YAML)
# ----------------------------------------------------------------------------

# Log Level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO (configured in yaml/core/brotr.yaml)
# LOG_LEVEL=INFO

# Environment Type
# Options: development, staging, production
# Default: development
# ENVIRONMENT=development

# ----------------------------------------------------------------------------
# MONITORING & METRICS (Optional)
# ----------------------------------------------------------------------------

# Enable Prometheus Metrics Export
# Options: true, false
# Default: false
# ENABLE_PROMETHEUS=false

# Prometheus Export Port
# Default: 9090
# PROMETHEUS_PORT=9090

# ----------------------------------------------------------------------------
# SERVICE-SPECIFIC CONFIGURATION (Optional - configured in service YAML)
# ----------------------------------------------------------------------------

# Finder Service
# FINDER_ENABLED=true
# FINDER_INTERVAL_SECONDS=3600

# Monitor Service
# MONITOR_ENABLED=true
# MONITOR_INTERVAL_SECONDS=300

# Synchronizer Service
# SYNCHRONIZER_ENABLED=true
# SYNCHRONIZER_BATCH_SIZE=100

# ----------------------------------------------------------------------------
# DEVELOPMENT & TESTING (Optional)
# ----------------------------------------------------------------------------

# Enable Debug Mode
# Options: true, false
# Default: false
# DEBUG=false

# Database Connection Pool Size (overrides YAML config)
# MIN_POOL_SIZE=10
# MAX_POOL_SIZE=100

# ----------------------------------------------------------------------------
# SECURITY NOTES
# ----------------------------------------------------------------------------
# NEVER commit the .env file to version control
# Add .env to .gitignore (already configured)
# Rotate DB_PASSWORD regularly in production
# Use strong passwords (12+ characters, mixed case, numbers, symbols)
# Restrict file permissions: chmod 600 .env
# ----------------------------------------------------------------------------
