# ============================================================================
# BigBrotr Environment Configuration
# ============================================================================
# Copy this file to .env and set your database password
# Command: cp .env.example .env
#
# Configuration Philosophy:
#   - ALL non-sensitive config comes from YAML files (yaml/core/brotr.yaml)
#   - ONLY sensitive data (passwords) comes from environment variables
#   - Docker uses brotr.docker.yaml (host=postgres) via --docker flag
#   - Local development uses brotr.yaml (host=localhost)
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE PASSWORD (REQUIRED)
# ----------------------------------------------------------------------------
# This is the ONLY required environment variable.
# All other configuration is loaded from YAML files.
#
# Security recommendations:
#   - Minimum 12 characters
#   - Mix of uppercase, lowercase, numbers, symbols
#   - Rotate regularly in production
#   - Never commit to version control
#   - Restrict file permissions: chmod 600 .env
# ----------------------------------------------------------------------------

DB_PASSWORD=your_secure_password_here

# ============================================================================
# THAT'S IT!
# ============================================================================
# All other configuration (host, port, database, user, timeouts, batch sizes,
# retry settings, etc.) is defined in YAML files:
#
#   yaml/core/brotr.yaml        - Local development (host=localhost)
#   yaml/core/brotr.docker.yaml - Docker containers (host=postgres)
#   yaml/services/*.yaml        - Service-specific configuration
#
# To modify settings, edit the YAML files directly.
# ============================================================================
